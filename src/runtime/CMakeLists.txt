set(sources

    aliases
    api
    check
    compiler
    debug
    definitions
    errors
    macros
    reader
    symbol_table
    trace
    types

)

_add_libchicken_sources(runtime ${sources})
