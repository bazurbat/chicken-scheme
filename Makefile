all:
	@echo "Sorry, Chicken can only be built with GNU make."
	@echo ""
	@echo "The '${MAKE}' command you just used is your system's default make"
	@echo "program, which apparently is not GNU Make."
	@echo ""
	@echo "Try 'gmake' or 'gnumake' instead."
